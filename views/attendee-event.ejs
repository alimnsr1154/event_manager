<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Event Details</title>
    <link rel="stylesheet" href="/main.css">
    <style>
        input[type="text"],
        input[type="number"],
        button {
            height: 40px;
            /* Adjust the height as needed */
            padding: 10px;
            font-size: 16px;
        }

        button {
            height: 50px;
            /* Adjust the height as needed */
            padding: 10px 20px;
            font-size: 16px;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>
            <%= event.title %>
        </h1>
        <div class="event-details">
            <p><strong>Description:</strong>
                <%= event.description %>
            </p>
            <p><strong>Date:</strong>
                <%= new Date(event.event_date).toLocaleString() %>
            </p>

            <h2>Ticket Information</h2>
            <p>Full Price Tickets: £<%= event.full_price_cost.toFixed(2) %> (Remaining: <%= event.remaining_full_price
                        %>)</p>
            <p>Concession Tickets: £<%= event.concession_price.toFixed(2) %> (Remaining: <%= event.remaining_concession
                        %>)</p>

            <form action="/events/book/<%= event.id %>" method="POST">
                <label for="attendee_name">Your Name:</label>
                <input type="text" id="attendee_name" name="attendee_name" required>

                <label for="full_price_tickets">Full Price Tickets:</label>
                <input type="number" id="full_price_tickets" name="full_price_tickets" min="0"
                    max="<%= event.remaining_full_price %>" required>

                <label for="concession_tickets">Concession Tickets:</label>
                <input type="number" id="concession_tickets" name="concession_tickets" min="0"
                    max="<%= event.remaining_concession %>" required>

                <button type="submit">Book Tickets</button>
            </form>

        </div>
    </div>
</body>

</html>